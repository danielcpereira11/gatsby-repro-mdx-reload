import { css } from "@emotion/core"
import { colors } from "@styles/variables"

const companiesBannerStyles = css`
  padding: 45px 0;
  background: ${colors.blackish};
`

export { companiesBannerStyles }
